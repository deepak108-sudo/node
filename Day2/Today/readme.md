-> node automatically take index.js from folder so no need to write file name.
-> you can keep all import in one .js file and then import directly no need to require individually
->JS is single threaded synchronous language.

Knowledge about threading, multithreading, single core, octa core, Parallelism, context switching, concurrent.